#include <stdio.h>
#include <stdlib.h>

// Datatype in C
int main() {
  int age = 10;
  double gpa = 3.6;
  char grade ='A';
  char phrase[] = "Giraffe";

  printf("%d, %f, %c, %s", age, gpa, grade, phrase);
}